name: Deploy to EC2

on:
  push:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE: ghcr.io/${{ github.repository_owner }}/wherewego-api

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build JAR
        working-directory: backend
        run: ./gradlew :apps:wherewego-api:bootJar -x test

      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE }}:latest
            ${{ env.IMAGE }}:${{ github.sha }}

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1
        env:
          ENV_FILE: ${{ secrets.ENV_FILE }}
          GH_PAT: ${{ secrets.GH_PAT }}
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          envs: ENV_FILE,GH_PAT
          script: |
            # ghcr.io 로그인 후 이미지 pull
            echo "$GH_PAT" | docker login ghcr.io -u rnqhstmd --password-stdin
            docker pull ${{ env.IMAGE }}:latest

            # 배포 시에만 사용하는 임시 env 파일 (배포 완료 후 즉시 삭제)
            ENV_TMP=$(mktemp)
            chmod 600 "$ENV_TMP"
            printf '%s' "$ENV_FILE" > "$ENV_TMP"

            # 컨테이너 교체
            docker stop wherewego-api || true
            docker rm   wherewego-api || true
            docker run -d \
              --name wherewego-api \
              --env-file "$ENV_TMP" \
              -e JAVA_TOOL_OPTIONS="-Xmx512m -Xms256m" \
              -p 8080:8080 \
              --memory 700m \
              --restart unless-stopped \
              ${{ env.IMAGE }}:latest

            # 임시 파일 즉시 삭제 (EC2에 잔류하지 않음)
            rm -f "$ENV_TMP"

            # 미사용 이미지 정리
            docker image prune -f
